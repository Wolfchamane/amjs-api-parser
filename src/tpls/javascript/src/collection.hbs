// Requires item type class
require('{{filePath itemType}}');
// --------------------------- /
const { AmjsDataTypesCollection } = require('@amjs/data-types');

/**
* @NOTICE: DO NOT MODIFY THIS FILE, generated with '@amjs/api-parser'
* @created {{date}}
*
* {{summary}}
* @namespace   {{dotSep namespace}}
* @class       {{dotSep className}}
* @extends     amjs.dataTypes.Object
*/
class {{className}} extends AmjsDataTypesCollection
{
    /**
     * @inheritDoc
     */
    constructor(values)
    {
        super();
        this.$itemType = '{{itemType}}';
        this.value = values;
    }
}

// Register and export collection class
AmjsDataTypesCollection.register('{{className}}', {{className}});
module.exports = {{className}};
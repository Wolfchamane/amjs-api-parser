{{#if requirements.length}}
// Required base data types
{{#each requirements as |path|}}
require('{{parseRequirement path}}');
{{/each}}
// ------------------------ /
{{/if}}
const { AmjsDataTypesObject } = require('@amjs/data-types');

/**
 * @NOTICE: DO NOT MODIFY THIS FILE, generated with '@amjs/api-parser'
 * @created {{date}}
 *
 * {{summary}}
 * @namespace   {{dotSep namespace}}
 * @class       {{dotSep className}}
 * @extends     amjs.dataTypes.Object
 */
class {{className}} extends AmjsDataTypesObject
{
    /**
     * @inheritDoc
     */
    constructor(values)
    {
        super();

        this.$propertyTypes = {
        {{#each properties as |prop|}}
            {{prop.name}}: '{{prop.type}}',
        {{/each}}
        };

        {{#each properties as |prop|}}
        this.{{prop.name}} = null;
        {{/each}}

        this._setProperties(values);
    }
}

// Register and export class
AmjsDataTypesObject.register('{{className}}', {{className}});
module.exports = {{className}};